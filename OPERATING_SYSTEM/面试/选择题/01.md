### 题1.1
```
进程调度时，下列进程状态的变化过程哪一项是不可能发生的？
A. 阻塞挂起 -> 就绪挂起
B. 就绪挂起 -> 阻塞挂起
C. 就绪挂起 -> 就绪
D. 阻塞挂起 -> 阻塞
```

### 题1.2
```
对进程和线程的描述，以下正确的是
A. 父进程里的所有线程共享相同的地址空间，父进程的所有子进程共享形同的地址空间
B. 改变进程里面主线程的状态会影响到其他线程的行为，改变父进程的状态不会影响到子进程
C. 多线程会引起死锁，而多进程不会
D. 以上都不正确
```
